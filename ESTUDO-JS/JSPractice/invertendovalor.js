//6. Use ! (negação) para inverter o valor de uma variável booleana.
let invertendovalor = true;

let bits = true;

console.log(bits);

bits = !bits;

console.log(bits);
if(!!invertendovalor){
    console.log('positivo');
}