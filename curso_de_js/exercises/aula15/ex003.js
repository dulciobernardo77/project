/*2. Iteração com Arrays**
4. Use um loop `for` para exibir todos os itens do array `frutas` no console.
5. Utilize o método `forEach` para exibir os itens do array com a seguinte mensagem: `"A fruta é: [fruta]"`.
 */
let frutas = ['maçã', 'banana', 'laranja'];
let c = 0;

while (c <= frutas.length) {
    console.log(frutas);
    c++;
}
