let inputusd = window.document.querySelector('input#usd');
let inputbrl = window.document.querySelecto('input#brl');

inputusd.addEventListener('keyup', () => {
    console.log('apertou no campo USD');
});

inputbrl.addEventListener('keyup', () => {
    console.log('apertou no campo BRL');
});
